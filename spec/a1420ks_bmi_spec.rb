require 'spec_helper'

describe A1420ksBmi do
  it 'has a version number' do
    expect(A1420ksBmi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
